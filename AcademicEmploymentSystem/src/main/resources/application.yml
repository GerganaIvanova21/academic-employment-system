server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/AcademicEmploymentSystem
    username: postgres
    password: Kyuubi279
    driver-class-name: org.postgresql.Driver

    jpa:
      hibernate:
        ddl_auto: create-drop
      show-sql: true
      properties:
        hibernate:
          format-sql: true
          dialect: org.hibernate.dialect.PostgreSQLDialect
        database: postgres
        database-platform: org.hibernate.dialect.PostgreSQLDialect

